{
  "name": "nemoris",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd backend && go run ./cmd/api",
    "build": "cd backend && go build -o bin/nemoris ./cmd/api",
    "start": "cd backend && ./bin/nemoris"
  }
}
